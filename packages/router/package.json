{
  "name": "@picos/router",
  "version": "0.0.3",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "fcli remove dist && tsc",
    "prepublishOnly": "run-s build",
    "dev": "tsc --watch --preserveWatchOutput"
  },
  "dependencies": {
    "url-pattern": "^1.0.3"
  },
  "peerDependencies": {
    "@picos/core": "0.0.3"
  },
  "devDependencies": {
    "file-cli": "^1.2.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.8.3"
  },
  "files": [
    "dist/**/*"
  ]
}
